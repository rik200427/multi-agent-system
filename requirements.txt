# ─────────────────────────────────────────────────────────────
# Multi-Agent Research Assistant — requirements.txt
# Verified conflict-free — pip auto-resolved May 2026
#
# Install: pip install -r requirements.txt
# Python:  3.10 / 3.11 / 3.12  (all work)
# ─────────────────────────────────────────────────────────────

# ── LangChain ecosystem (must stay on same major version) ────
langchain==1.3.1
langchain-core==1.4.0
langchain-groq==0.3.2
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langsmith==0.8.5

# ── OpenAI ──────────────────────────────────────────────────
groq==0.28.0

# ── Search ──────────────────────────────────────────────────
tavily-python==0.7.24

# ── Scraping ────────────────────────────────────────────────
beautifulsoup4==4.12.3
httpx==0.28.1
requests==2.34.2
lxml==5.3.0

# ── Async / HTTP internals ───────────────────────────────────
anyio==4.13.0
httpcore==1.0.9
sniffio==1.3.1

# ── Data / Utilities ────────────────────────────────────────
python-dotenv==1.0.1
tenacity==9.1.4
joblib==1.4.2
pydantic==2.13.4
tiktoken==0.13.0

# ── Frontend ────────────────────────────────────────────────
streamlit==1.57.0

# ── Persistence (sqlite3 is built-in — no install needed) ───

# ─────────────────────────────────────────────────────────────
# Optional — uncomment only if you add these features
# ─────────────────────────────────────────────────────────────

# LangGraph (upgraded orchestration — already pulled in above)
# langgraph==1.2.1

# Playwright (JS-rendered page scraping)
# playwright==1.46.0
# after install run: playwright install chromium

# LangSmith tracing UI (already pulled in above as langsmith)
# just set LANGCHAIN_API_KEY in your .env to enable